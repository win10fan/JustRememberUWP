<Page
    x:Class="JustRemember.Views.Home"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JustRemember.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

	<Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
		<Grid.RowDefinitions>
			<RowDefinition Height="50"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="50"/>
		</Grid.RowDefinitions>
		<Rectangle Fill="{ThemeResource SystemControlBackgroundAccentBrush}"/>
		<StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center">
			<ToggleButton Content="&#xE160;" Width="60" Height="50" Style="{StaticResource HeaderNav}" FontFamily="Segoe MDL2 Assets" FontSize="24">
				
			</ToggleButton>
		</StackPanel>
	</Grid>
</Page>
